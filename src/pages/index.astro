---
import type { MarkdownInstance } from 'astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import PostList from '../components/PostList.astro';
import type { Frontmatter } from '../utils/posts';
import { filterPublicPosts, sortPostsByDate } from '../utils/posts';

// Get all posts from the central posts directory
let allPosts: MarkdownInstance<Frontmatter>[] = [];
try {
	// We want to get all markdown files from the posts directory
	const posts = Object.values(import.meta.glob<MarkdownInstance<Frontmatter>>('./posts/*.md', { eager: true }));
	// Filter out private posts
	allPosts = filterPublicPosts(posts);
} catch (error) {
	console.log("Error loading posts:", error);
}

// Sort posts by date, most recent first
const sortedPosts = sortPostsByDate(allPosts);

// Get the 3 most recent posts
const recentPosts = sortedPosts; //.slice(0, 3);

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<BaseLayout title="Home">
	<div class="home-content">
			<div > 
			  <div class="page-content">
				<h1 class="typewriter" id="typewriter"></h1>
				<p>Or rather, his digital twin.<br />Building the future of EdTech.<br />And sometimes write about artsy stuff.</p>
			  </div>

			</div>
			<hr class="section-divider" />
			<PostList
				posts={recentPosts}
				emptyMessage="No posts available yet. Check back soon!"
			/>
		</div>
	</div>
</BaseLayout>

<script>
  const el = document.getElementById('typewriter');
  const text = "Hi, I'm ";
  const name = "Ammar";
  let i = 0;
  const full = text + name;

  function type() {
    if (i <= full.length) {
      let html = '';
      const typed = full.slice(0, i);
      if (i > text.length) {
        html = text + '<span class="name-highlight">' + typed.slice(text.length) + '</span>';
      } else {
        html = typed;
      }
      html += '<span class="cursor">&nbsp;</span>';
      el!.innerHTML = html;
      i++;
      setTimeout(type, 80);
    } else {
      // Remove cursor after a short pause
      setTimeout(() => {
        el!.innerHTML = text + '<span class="name-highlight">' + name + '</span>';
      }, 1500);
    }
  }
  type();
</script>
